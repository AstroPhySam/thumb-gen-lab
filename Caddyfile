{
    auto_https disable_redirects
}

{$DOMAIN} {
    root * /srv/www

    handle /api/* {
        reverse_proxy api:8000
    }

    handle_path /healthz {
        reverse_proxy api:8000
    }

    file_server
}