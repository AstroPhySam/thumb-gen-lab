# Rendered by Ansible from GitHub Actions secrets (never edit on the server).
MINIO_ENDPOINT=s3.{{ minio_region }}.amazonaws.com
MINIO_ACCESS_KEY={{ minio_access_key }}
MINIO_SECRET_KEY={{ minio_secret_key }}
MINIO_REGION={{ minio_region }}
MINIO_SECURE=true
MINIO_BUCKET_ORIGINALS={{ minio_bucket_originals }}
MINIO_BUCKET_THUMBNAILS={{ minio_bucket_thumbnails }}
ECR_REGISTRY={{ ecr_registry }}
DOMAIN={{ domain }}
CORS_ORIGINS=[]